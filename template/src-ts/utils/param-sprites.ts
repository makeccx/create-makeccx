import { type } from "clipcc-extension";
import { menuSprites } from "./menu-sprites";

export const paramSprites: MyParam = {
    type: type.ParameterType.STRING,
    defaultValue: undefined,
    menu: menuSprites,
}